import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  background: {
    flex: 1,
    alignItems: 'stretch',
    justifyContent: 'center'
  },
  contentBackground: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },
  textStyle: {
    fontSize: 50,
    fontWeight: 'bold'
  }
});

export default styles;
