import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  background: {
    padding: 10,
    flex: 0,
    alignItems: 'stretch',
    justifyContent: 'center',
    backgroundColor: '#006080'
  },
  textStyle: {
    fontSize: 20,
    fontWeight: 'bold'
  }
});

export default styles;
